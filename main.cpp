#include <iostream>
#include <vector>

int main() {

    // Initialize a vector of length 5, all the elements are 0
    std::vector<int> vec(5,0);
    
    // 3 by 4 matrix
    std::vector<std::vector<int> > vec2d(3, std::vector<int>(4, 0));

    return 0;
}
